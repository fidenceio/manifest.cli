# Changelog v38.0.0

**Release Date:** 2026-04-04 15:25:17 UTC
**Release Type:** major

## New Features

- Fleet documentation orchestration module (`manifest-fleet-docs.sh`)
- Configurable docs placement strategy: `"fleet-root"`, `"per-service"`, or `"both"`
- Fleet-root summary docs with aggregated service versions and changes
- Fleet-root index mode with lightweight version list and relative links
- `manifest fleet docs` command with `generate`, `status`, and `help` subcommands
- CLI flags: `--fleet-only`, `--services-only`, `--strategy <s>` for override control
- Granular document type toggles (release notes, changelog, index, README version)

## Improvements

- Fleet ship workflow now includes docs generation step (non-blocking)
- Dynamic step numbering in `fleet_ship()` for cleaner progress output
- Fleet config module extended with 10 new defaults and YAML path mappings for docs
- Updated `manifest.fleet.yaml.example` with full `docs:` configuration section
- Fleet help text updated to reflect new `fleet docs` subcommands

## Changes

- `manifest fleet docs` no longer shows "not yet implemented" — fully operational
- Fleet ship flow: `prep -> docs -> pr create -> pr queue` (docs step added)

## Documentation

- Updated fleet YAML example with docs strategy, fleet_root, per_service, and generate sections
- Fleet docs help text with usage examples and configuration reference

---
*Generated by Manifest CLI v38.0.0*
