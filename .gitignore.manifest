# =============================================================================
# Manifest CLI
# =============================================================================
.manifest-cli/
*.manifest-cli.log

# =============================================================================
# OS generated files
# =============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# =============================================================================
# Editor and IDE files
# =============================================================================
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace
.project
.classpath
.settings/
*.tmproj
*.tmproject
.tmtags
nbproject/

# =============================================================================
# AI assistants and coding agents
# =============================================================================
# Per-developer agent workspaces: sessions, transcripts, caches, local settings.
# Tools whose dir holds BOTH team-shared and personal config are ignored by
# CONTENT (dir/*) so the shared subset can be re-included — git cannot re-include
# a path inside an excluded directory.
.aider*
.amazonq/
.augment/
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/skills/
!.claude/settings.json
.cline/
.codeium/
.continue/
.crush/
.cursor/*
!.cursor/rules/
.gemini/
.goose/
.junie/
.kiro/
.opencode/
.qodo/
.roo/
.specstory/
.tabnine/
.trae/
.windsurf/*
!.windsurf/rules/
!.windsurf/workflows/

# =============================================================================
# Environment, secrets, and local/generated config
# =============================================================================
.env
.env.*
*.local.yaml
*.local.yml
*.local.json
*.local.toml
*.secret.yaml
*.secret.yml
*.secret.json
*.secret.*
# Keep authoring templates trackable — example/template variants stay in git.
!.env.example
!.env.template
!*.example.yaml
!*.example.yml
!*.example.json
!*.example.toml
!*.template.yaml
!*.template.yml
!*.template.json
!*.template.toml

# ============================================================================
#  KEY MATERIAL
# ============================================================================
# Deny-by-default. Manifest's release commit is a bare `git add .` and its
# security gate does not scan for key material, so an unmatched private key
# gets pushed to the remote permanently, on the next ship, silently.
#
# Escape hatch for a legitimately-public file caught below:
#     git add -f path/to/public-thing.pem

# --- PEM-family containers: the extension does not tell you what's inside ---
*.key
*.pem
*.p8
*.pkcs8
*.der

# --- Binary bundles: these always carry the private half ---
*.pfx
*.p12
*.jks
*.keystore
*.bks

# --- SSH private keys ---
id_rsa
id_dsa
id_ecdsa
id_ed25519
id_ecdsa_sk
id_ed25519_sk
*_rsa
*_ed25519
*.ppk

# --- GPG private keyrings only. NOT *.gpg — that is ciphertext and is often
#     committed deliberately (git-crypt, sops, pass). ---
secring.*
*.private.gpg
*.secret.gpg

# ----------------------------------------------------------------------------
#  EXCEPTIONS — public halves, re-included. Must stay AFTER the rules above.
# ----------------------------------------------------------------------------
!*.pub
!*public*.pem
!*public*.key
!fullchain.pem
!chain.pem

# =============================================================================
# Logs and runtime data
# =============================================================================
*.log
logs/
pids/
*.pid
*.seed
*.pid.lock

# =============================================================================
# Dependencies
# =============================================================================
node_modules/
bower_components/
vendor/
.bundle/
jspm_packages/

# =============================================================================
# Package manager caches and artifacts
# =============================================================================
.npm
.yarn/
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnpm-store/
.node_repl_history
*.tgz
.yarn-integrity

# =============================================================================
# Build outputs
# =============================================================================
dist/
build/
out/
target/
*.egg-info/
*.egg
*.whl
*.class
*.jar
*.war
*.ear

# =============================================================================
# Test and coverage
# =============================================================================
coverage/
.nyc_output/
.coverage
htmlcov/
.pytest_cache/
.tox/
.nox/
nosetests.xml
coverage.xml
*.cover
*.py,cover

# =============================================================================
# Compiled and generated files
# =============================================================================
*.o
*.so
*.dylib
*.dll
*.exe
*.out
*.app
*.com
__pycache__/
*.py[cod]
*$py.class
*.class

# =============================================================================
# Temporary files
# =============================================================================
tmp/
temp/
*.tmp
*.temp
*.bak
*.orig
*.rej

# =============================================================================
# Archive directories
# =============================================================================
zArchive/
archive/

# =============================================================================
# Terraform
# =============================================================================
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# =============================================================================
# Docker
# =============================================================================
.docker/

# =============================================================================
# Miscellaneous
# =============================================================================
*.sqlite
*.db
.cache/
.parcel-cache/
.turbo/
.next/
.nuxt/
.output/
.svelte-kit/
