# =============================================================================
# 🎯 Project-Specific Configuration Override
# =============================================================================
# This file shows how to override global Manifest CLI settings for this project
#
# 📋 HOW TO USE:
#   1. Copy this file: cp env.manifest.local.example .env.manifest.local
#   2. Uncomment and customize the variables you want to override
#   3. This .env.manifest.local file will override your global .env.manifest.global settings
#
# 🔒 IMPORTANT: .env.manifest.local is automatically ignored by git
#   Your project-specific settings stay local and private
#
# 📦 FOR FLEET/POLYREPO: See the "FLEET CONFIGURATION" section below
# =============================================================================

# =============================================================================
# 🚀 MOST COMMON OVERRIDES
# =============================================================================
# Uncomment and customize these for immediate project-specific needs:

# MANIFEST_CLI_GIT_DEFAULT_BRANCH="develop"                    # Different default branch
# MANIFEST_CLI_VERSION_FORMAT="YYYY.MM.DD"                    # Date-based versioning
# MANIFEST_CLI_DOCS_FOLDER="project-docs"                     # Custom docs folder
# MANIFEST_CLI_DOCS_ARCHIVE_FOLDER="project-docs/archive"     # Custom archive location

# =============================================================================
# 🌍 TIMEZONE OVERRIDE
# =============================================================================
# Override the global timezone setting for this project
# Use IANA timezone names: America/New_York, America/Los_Angeles, Europe/London, Asia/Tokyo, etc.

# MANIFEST_CLI_TIMEZONE="UTC"

# =============================================================================
# 🎮 DEVELOPMENT OVERRIDES
# =============================================================================
# Control prompts and debugging for this project:

# MANIFEST_CLI_DEBUG=true                                     # Enable debug mode
# MANIFEST_CLI_VERBOSE=true                                   # Enable verbose output
# MANIFEST_CLI_INTERACTIVE_MODE=true                          # Enable interactive prompts

# =============================================================================
# 🏢 PROJECT METADATA
# =============================================================================
# Add project-specific information:

# MANIFEST_CLI_PROJECT_NAME="My Awesome Project"
# MANIFEST_CLI_PROJECT_DESCRIPTION="A revolutionary tool for developers"
# MANIFEST_CLI_TEAM_NAME="Development Team"
# MANIFEST_CLI_ORGANIZATION="Acme Corp"

# =============================================================================
# 📝 QUICK EXAMPLES
# =============================================================================
#
# 🎯 Open Source Project:
# MANIFEST_CLI_GIT_DEFAULT_BRANCH="main"
# MANIFEST_CLI_VERSION_FORMAT="XX.XX.XX"
#
# 🎯 Enterprise Project:
# MANIFEST_CLI_VERSION_FORMAT="XXXX.XXXX.XXXX"
# MANIFEST_CLI_DOCS_FOLDER="documentation"
#
# 🎯 Debug Development:
# MANIFEST_CLI_DEBUG=true
# MANIFEST_CLI_VERBOSE=true
# MANIFEST_CLI_INTERACTIVE_MODE=true
#
# =============================================================================
# 🔧 TROUBLESHOOTING
# =============================================================================
# ❌ Override not working? Check that .env.manifest.local is in project root
# ❌ Variable not recognized? Ensure it starts with MANIFEST_CLI_
# ❌ Still using global settings? Run 'manifest config' to verify
# =============================================================================

# =============================================================================
# 🚢 FLEET CONFIGURATION (Polyrepo Management)
# =============================================================================
# Configure Manifest CLI's fleet/polyrepo features
# For full fleet setup, also see: manifest.fleet.yaml.example
#
# Fleet mode allows managing multiple related repositories together:
#   - Coordinated version bumps across services
#   - Unified changelogs aggregating all services
#   - Dependency tracking and compatibility matrices
#   - Parallel operations for faster releases

# =============================================================================
# 🔍 FLEET DETECTION
# =============================================================================

# How to determine if this repo is part of a fleet
# "auto"  = Detect from manifest.fleet.yaml presence (default)
# "true"  = Force fleet mode (look for fleet root)
# "false" = Force single-repo mode (ignore any fleet.yaml)
# MANIFEST_CLI_FLEET_MODE="auto"

# Explicit path to fleet root (optional)
# Leave empty for auto-detection (walks up directory tree looking for manifest.fleet.yaml)
# MANIFEST_CLI_FLEET_ROOT=""

# =============================================================================
# 🏷️  FLEET IDENTITY (for services within a fleet)
# =============================================================================
# These settings identify this service within the fleet

# This service's name (must match key in manifest.fleet.yaml services section)
# MANIFEST_CLI_FLEET_SERVICE_NAME=""

# Exclude this service from `manifest fleet go` operations
# Use case: Legacy service being deprecated, or externally managed
# MANIFEST_CLI_FLEET_EXCLUDE_FROM_BUMP="false"

# Team ownership (appears in changelogs and notifications)
# MANIFEST_CLI_FLEET_TEAM=""

# Service type: service | library | infrastructure | tool | external
# MANIFEST_CLI_FLEET_SERVICE_TYPE="service"

# =============================================================================
# 📝 FLEET CHANGELOG
# =============================================================================

# Generate unified fleet changelog (CHANGELOG_FLEET.md)
# MANIFEST_CLI_FLEET_CHANGELOG_UNIFIED="true"

# Detail level in unified changelog
# "full"    = Include entire per-service changelog
# "summary" = Key sections only (New Features, Breaking Changes, Bug Fixes)
# "minimal" = Just version number and one-line summary
# MANIFEST_CLI_FLEET_CHANGELOG_DETAIL="summary"

# Include dependency compatibility matrix in fleet changelog
# MANIFEST_CLI_FLEET_CHANGELOG_MATRIX="true"

# Include NTP verification details in fleet changelog
# MANIFEST_CLI_FLEET_CHANGELOG_NTP="true"

# =============================================================================
# ⚙️  FLEET OPERATIONS
# =============================================================================

# Default bump type for `manifest fleet go` (if not specified)
# MANIFEST_CLI_FLEET_DEFAULT_BUMP="patch"

# Run fleet operations in parallel for speed
# MANIFEST_CLI_FLEET_PARALLEL="true"

# Maximum number of parallel operations
# MANIFEST_CLI_FLEET_MAX_PARALLEL="4"

# Commit strategy
# "per-service" = One commit per service (cleaner history)
# "atomic"      = Single commit referencing all changes (monorepo-like)
# MANIFEST_CLI_FLEET_COMMIT_STRATEGY="per-service"

# Push strategy
# "immediate" = Push each service as it completes
# "batched"   = Push all services at end (default, safer)
# "manual"    = Don't push automatically
# MANIFEST_CLI_FLEET_PUSH_STRATEGY="batched"

# =============================================================================
# 📦 FLEET SUBMODULES
# =============================================================================

# How to handle git submodules in fleet services
# "include"  = Include submodules in operations
# "exclude"  = Ignore submodules
# "separate" = Treat submodules as separate fleet services
# MANIFEST_CLI_FLEET_SUBMODULE_HANDLING="include"

# Submodule update strategy during sync
# "checkout" = git submodule update --checkout (default)
# "rebase"   = git submodule update --rebase
# "merge"    = git submodule update --merge
# MANIFEST_CLI_FLEET_SUBMODULE_UPDATE="checkout"

# =============================================================================
# ✅ FLEET VALIDATION
# =============================================================================

# Require clean git status before fleet operations
# MANIFEST_CLI_FLEET_REQUIRE_CLEAN="true"

# Enforce dependency version constraints
# Fails if a service's dependencies aren't satisfied
# MANIFEST_CLI_FLEET_ENFORCE_DEPS="true"

# Allow fleet operations on non-default branches
# MANIFEST_CLI_FLEET_ALLOW_BRANCH_OPS="false"

# Strict mode: treat warnings as errors
# MANIFEST_CLI_FLEET_STRICT="false"

# =============================================================================
# 🔔 FLEET NOTIFICATIONS
# =============================================================================

# Alert when a library has a breaking change (major version bump)
# MANIFEST_CLI_FLEET_ALERT_BREAKING="true"

# Alert method: "console" | "slack" | "github-issue"
# MANIFEST_CLI_FLEET_ALERT_METHOD="console"

# Slack webhook URL (if using slack alerts)
# MANIFEST_CLI_FLEET_SLACK_WEBHOOK=""

# =============================================================================
# 📋 FLEET QUICK EXAMPLES
# =============================================================================
#
# 🎯 Service in a fleet (basic):
#   MANIFEST_CLI_FLEET_MODE="auto"
#   MANIFEST_CLI_FLEET_SERVICE_NAME="user-service"
#   MANIFEST_CLI_FLEET_TEAM="auth-team"
#
# 🎯 Service excluded from fleet bumps:
#   MANIFEST_CLI_FLEET_EXCLUDE_FROM_BUMP="true"
#
# 🎯 Force single-repo mode (ignore fleet):
#   MANIFEST_CLI_FLEET_MODE="false"
#
# 🎯 Library service (triggers breaking change alerts):
#   MANIFEST_CLI_FLEET_SERVICE_TYPE="library"
#   MANIFEST_CLI_FLEET_ALERT_BREAKING="true"
#
# =============================================================================